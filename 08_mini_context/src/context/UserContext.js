import React from 'react';

const UserContext = React.createContext();

export default UserContext;

/**
 * 1. Create a new js file (UserContext.js)
 * 2. Import React from react and use it create context ``` const UserContext = React.createContext();```
 * 3. Export the created context
 */
const notes = ''